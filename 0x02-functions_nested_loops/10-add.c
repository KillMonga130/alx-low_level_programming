#include "main.h"

/**
 * add - adds two integers and returns the results
 * @y: number being added
 * @x: number beung added
 * Return: the sum of the integers
 */

int add(int x, int y)
{
	return (x + y);
}
