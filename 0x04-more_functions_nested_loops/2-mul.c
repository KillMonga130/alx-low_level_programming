#include "main.h"

/**
 * mul - Multipy two integers
 * @a: first integer
 * @b: Second integer
 * Return: multiplication of the two integers
 */

int mul(int a, int b)
{
	int mult;

	mult = (a * b);
	return (mult);
}
